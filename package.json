{
  "name": "glimpse-of-epiphany",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "storybook": "turbo run storybook",
    "gen:db-types": "supabase gen types typescript --project-id \"${SUPABASE_PROJECT_ID:?set SUPABASE_PROJECT_ID (see .env.example)}\" --schema public > packages/api/src/database.types.tmp && mv packages/api/src/database.types.tmp packages/api/src/database.types.ts || rm -f packages/api/src/database.types.tmp",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "supabase": "^2.63.1",
    "turbo": "^2.10.5"
  }
}
